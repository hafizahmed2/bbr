<%= render 'shared/back_link_section'%>

<section class="input-form-group">
  <div class="container">
    <div class="d-flex justify-content-center" id="bookings-page">
      <div class="support-image-box-left">
        <%= image_tag 'card-big-img-3.jpg', alt: 'Booking page image' %>
      </div>

      <div class="support-box-right" id="booking_form_area">
        <%= render 'bookings/booking_form' %>
      </div>
    </div>
  </div>
</section>

<div id="success_popup_area">
  <%= render "shared/request_success_pop_up" %>
</div>
